# TITAN Multi-Provider Configuration Template
# Copy this to .env and fill in your actual keys
#
# ⚠️  SUBSCRIPTION-ONLY MODE: This project is configured to use ONLY subscription
#     authentication. Pay-per-token API keys are BLOCKED by default.

# ==================== SUBSCRIPTION ENFORCEMENT ====================
# These flags BLOCK any pay-per-token API keys from being used
SUBSCRIPTION_ONLY_MODE=true
REJECT_API_KEY_AUTH=true

# ==================== CLAUDE CODE PRO MAX SUBSCRIPTION ====================
# REQUIRED: Use Claude Code PRO MAX subscription ($200/month) via OAuth
#
# AUTOMATIC SETUP (recommended):
#   Run: npm run auth:setup
#   This will automatically run 'claude setup-token' and populate this file
#
# MANUAL Setup Steps:
# 1. Ensure you have Claude Code PRO MAX subscription active
# 2. Run: claude login  (authenticates in browser)
# 3. Run: claude setup-token  (generates OAuth token)
# 4. Copy the token below (or let auth:setup do it automatically)
#
# The OAuth token uses your subscription - NO per-token API costs
CLAUDE_CODE_OAUTH_TOKEN=
#
# Latest Model: Claude Opus 4.5 (November 2025) - ALWAYS use latest
ANTHROPIC_MODEL=claude-opus-4-5-20251101
#
# ❌ BLOCKED: Do NOT set ANTHROPIC_API_KEY - it will cause an error!
# ❌ BLOCKED: Do NOT use claude-agent-sdk with API keys - use OAuth only!

# ==================== GOOGLE AI PRO/ULTRA SUBSCRIPTION ====================
# REQUIRED: Use Google AI Pro ($19.99/mo) or Ultra ($124.99/mo) via OAuth
# Account: cedricable@gmail.com
#
# Setup Steps:
# 1. Subscribe at: https://gemini.google/subscriptions/
# 2. Go to Google Cloud Console: https://console.cloud.google.com/
# 3. Create a new project or select existing
# 4. Enable "Generative Language API"
# 5. Configure OAuth consent screen:
#    - User Type: External
#    - Add scope: https://www.googleapis.com/auth/generative-language
#    - Add test user: cedricable@gmail.com
# 6. Create OAuth 2.0 Client ID (Desktop application)
# 7. Download and extract client_id and client_secret
#
# Detailed guide: https://ai.google.dev/gemini-api/docs/oauth
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
#
# Latest Model: Gemini 3 Pro (November 2025)
GOOGLE_AI_MODEL=gemini-3-pro-preview
#
# ❌ BLOCKED: Do NOT set GOOGLE_AI_API_KEY - it will cause an error!
# (Pay-per-token API keys are disabled in subscription-only mode)

# ==================== E2B SANDBOX (Free Tier Available) ====================
# E2B provides 100 hours/month free - this is NOT pay-per-token
# Get your key from: https://e2b.dev/docs
# ✅ ALLOWED: E2B API key is permitted
E2B_API_KEY=

# ==================== OPENROUTER (ALLOWED) ====================
# OpenRouter is ALLOWED for additional model access
# Get your key from: https://openrouter.ai/keys
# ✅ ALLOWED: OpenRouter API key is permitted
OPENROUTER_API_KEY=

# ==================== AGENTIC FLOW CONFIGURATION ====================
# Primary LLM provider: anthropic, google, openrouter
AGENTIC_FLOW_PRIMARY_PROVIDER=anthropic

# Secondary LLM provider (for fallback/consensus)
AGENTIC_FLOW_SECONDARY_PROVIDER=google

# Strategy: single, consensus, parallel, cascade
AGENTIC_FLOW_STRATEGY=consensus

# ==================== E2B SANDBOX CONFIGURATION ====================
E2B_TIMEOUT=300
E2B_TEMPLATE=base
E2B_METADATA_PROJECT=titan-ran

# ==================== DEVPOD CONFIGURATION ====================
DEVPOD_PROVIDER=docker
DEVPOD_CPU_LIMIT=4
DEVPOD_MEMORY_LIMIT=8G

# ==================== PLATFORM CONFIGURATION ====================
# Architecture: arm64 (Mac Silicon) or amd64
PLATFORM_ARCH=arm64

# Runtime mode: local, devpod, cloud
RUNTIME_MODE=local

# ==================== LOGGING & MONITORING ====================
LOG_LEVEL=info
ENABLE_TELEMETRY=true
AGENTDB_PERSISTENCE=true

# ==================== SECURITY ====================
# Enable quantum-resistant signatures
QUANTUM_SIGNATURES=true

# Sandbox isolation level: strict, moderate, relaxed
SANDBOX_ISOLATION=strict
